# Editor & CI config
.vscode/**
.vscode-test/**
.github/**

# Dependencies & build output — the .vsix ships dist/, never its inputs
node_modules/**
out/**
coverage/**

# Source & build tooling
src/**
esbuild.js
**/tsconfig.json
**/eslint.config.mjs
**/*.ts
**/*.map

# Test & QA harness
qa/**
**/.vscode-test.*
.extester/**
.mocharc-ui.js

# Repo config with no runtime role
.gitignore
.gitattributes
package-lock.json

# Maintainer docs — written for contributors, never shipped to users
CLAUDE.md
SPEC.md
SUPPORT.md
ROADMAP.md

# Local-only working trees — agent state, private notes, design libraries.
# Self-ignored from git, so they never appear in `git status` — but vsce packages
# from disk, not from git, and would otherwise ship them. Must stay named here.
.claude/**
.remember/**
process/**
import-statement-design/**
launch/**

# Demo media — the README loads these from GitHub, so the package stays lean.
# images/icon.png is the Marketplace icon and must NOT be excluded.
images/*.gif
images/*.mov
images/icon-original.png
images/social-preview.png

# OS cruft
**/.DS_Store
