% sample.tex — empty stub fixture; .tsx accepts every source (gating passes) but .tex has no primary/fallback/asset case → default: null → empty snippet, keyed on the extension not the bytes (tsx.md §1.19)
