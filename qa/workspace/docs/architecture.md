# Architecture

High-level view of the demo application architecture.

## Layers

- Presentation — React components and Angular templates.
- Domain — data models in `src/models/`.
- Infrastructure — API client and HTTP wrapper.
