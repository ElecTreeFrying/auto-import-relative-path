# qa/checklists/CLAUDE.md

Each file in this directory is a manual QA checklist. Naming convention: `{language}.md` maps to `workspace/{language}/`.

## Files

| File | Scope |
|------|-------|
| `general.md` | Cross-destination shared behavior — clipboard validation, same-file rejection, notifications, toast buttons, edge cases, path computation, extension stripping, DnD universal behaviors, Pick Style / Set Default QuickPick mechanics, settings mid-session. Run first; per-destination checklists assume it passed. |
| `javascript.md` | `.js` destination — gating matrix, 7 import styles + style-name drift, no smart identifiers (N/A for `.js`), placement modes, Pick Style / Set Default, drag-and-drop, edge cases. |
| `typescript.md` | `.ts` destination — gating matrix, 7 import styles + style-name drift, smart identifiers (exported-class detection + Angular PascalCase), placement modes, Pick Style / Set Default (TS-specific), drag-and-drop (TS-specific), edge cases. |
| `jsx.md` | `.jsx` destination — accept-all gating (every source; 0 rejections), two-arm style model via source-extension dispatch (7 JavaScript styles for `.js`/`.jsx` sources **or** 4 fixed asset shapes; `.ts`/`.tsx` → empty snippet) + style-name drift, no smart identifiers (§5 N/A), Bottom/Top/Cursor placement incl. leading-`*`-is-comment, shared `javascriptImportStyle`, Pick Style / Set Default, drag-and-drop incl. raw-text fallback. |
| `tsx.md` | `.tsx` destination — accept-all gating (every source; 0 rejections), two-script-arm + asset style model (7 TypeScript styles for `.ts`/`.tsx` sources **or** 7 JavaScript styles for `.js`/`.jsx` via fallback **or** 4 fixed asset shapes; every source renders non-empty — no empty-snippet case) + style-name drift, Angular-only smart identifiers (style 0; no exported-class fill), generic placement incl. leading-`*`-is-comment, shared `typescriptImportStyle`/`javascriptImportStyle`, Pick Style / Set Default, drag-and-drop (no raw-text fallback), markdown-star `.mdx`≠`.tsx` edge. |
| `mdx.md` | `.mdx` destination — accept-all gating (every source; 0 rejections), two-script-arm + asset style model (7 TypeScript styles for `.ts`/`.tsx` sources **or** 7 JavaScript styles for `.js`/`.jsx` via fallback **or** 4 fixed asset shapes; every source renders non-empty — no empty-snippet case) + style-name drift, Angular-only smart identifiers (style 0; no exported-class fill), generic placement incl. leading-`*`-**is-content**, shared `typescriptImportStyle`/`javascriptImportStyle`, Pick Style / Set Default, drag-and-drop (no raw-text fallback), markdown-star `.mdx`≠`.tsx` edge. |
| `html.md` | `.html` destination — allow-list gating (`.js` `.css` + image/video/audio/`.vtt`; `.html`→`.html` rejected), 6-way source-type dispatch (`<script>`/`<img>`/`<video>`/`<audio>` configurable + fixed `<link>`/`<track>`), no smart identifiers (§5 N/A), `forced-cursor` placement, always-keep-extension, Pick Style / Set Default, drag-and-drop, comment-marker mismatch edges. |
| `markdown.md` | `.md` destination — allow-list gating (`.md` + image; `.md`→`.md` accepted, media/`.vtt`/data/stylesheets rejected), 2-way source-type dispatch (fixed `[text](…)` link + 3 `markdownImageImportStyle` image styles) + style-name drift, no smart identifiers (§5 N/A), `forced-cursor` placement (setting ignored, column follows cursor, markdown-star quirk), always-keep-extension, Pick Style / Set Default, drag-and-drop, markdown-star vs `.tsx` edge. |
| `css.md` | `.css` destination — allow-list gating (`.css` + image), 2 `@import` styles + fixed image `url()` arm + style-name drift, no smart identifiers (§5 N/A), placement modes + inline-`url()`, Pick Style / Set Default, drag-and-drop, shared `@use`/`@forward` marker edges. |
| `scss.md` | `.scss` destination — allow-list gating (`.scss` + `.css` + image), 5 `@use`/`@forward`/`@import` styles + fixed image `url()` arm + style-name drift, partial-`_` normalization, `preserveStylesheetFileExtension` toggle, no smart identifiers (§5 N/A), placement modes + inline-`url()`, Pick Style / Set Default, drag-and-drop, edge cases. |
| `vue.md` | `.vue` destination — allow-list gating (accept-vs-reject; first framework-trio destination, shares `framework-component.ts`), one-table two-arm style model (7 TypeScript styles for all four script sources `.ts`/`.tsx`/`.js`/`.jsx` — `.js`/`.jsx` route to the **TS** builder, not JS — **or** a fixed asset shape), Angular-only smart identifiers (style 0; no exported-class fill; fires for all four script exts), SFC `<script>`-block placement, shared `typescriptImportStyle` (no `vueImportStyle`), Pick Style / Set Default, drag-and-drop incl. unsupported-pair raw-text fallback, `.vue`→`.vue` named-asset quirk. |
| `astro.md` | `.astro` destination — allow-list gating (**widest** framework accept-list; 16 accept / 5 reject; third framework-trio destination, shares `framework-component.ts`), one-table two-arm style model (7 TypeScript styles for all four script sources `.ts`/`.tsx`/`.js`/`.jsx` — `.js`/`.jsx` route to the **TS** builder, not JS — **or** a fixed asset shape), Angular-only smart identifiers (style 0; no exported-class fill; fires for all four script exts), frontmatter `---`-fence placement (no `<script setup>`/`<script context="module">`; create-if-missing wrapper), shared `typescriptImportStyle` (no `astroImportStyle`), Pick Style / Set Default, drag-and-drop incl. unsupported-pair raw-text fallback, named-asset family (`.astro`/`.vue`/`.svelte`/`.md`/`.mdx`→`.astro`). |
| `svelte.md` | `.svelte` destination — allow-list gating (accept-vs-reject; second framework-trio destination, shares `framework-component.ts`), one-table two-arm style model (7 TypeScript styles for all four script sources `.ts`/`.tsx`/`.js`/`.jsx` — `.js`/`.jsx` route to the **TS** builder, not JS — **or** a fixed asset shape), Angular-only smart identifiers (style 0; no exported-class fill; fires for all four script exts), SFC `<script>`-block placement (no `<script setup>`; `<script context="module">` tier-3 fallback), shared `typescriptImportStyle` (no `svelteImportStyle`), Pick Style / Set Default, drag-and-drop incl. unsupported-pair raw-text fallback, `.svelte`→`.svelte` named-asset quirk. |

## Rules

- **Checklist changes propagate.** When a checklist is added, removed, or updated, update surrounding docs and the workspace. Full propagation list is in [`../CLAUDE.md`](../CLAUDE.md) under "Propagation rule."
- **Checklist → workspace sync.** When a checklist references a fixture file, that file must exist in `workspace/{language}/`. After editing a checklist, verify the workspace has every referenced path. Add missing fixtures.
- **No general.md duplication.** Per-destination checklists must not re-test behaviors covered by `general.md` (clipboard validation, same-file rejection, notification wording, toast button actions, path computation, extension stripping, DnD same-file / notification behavior, Pick Style / Set Default QuickPick mechanics, settings mid-session). If a behavior is destination-neutral, it belongs in `general.md`.
- **Automated-test coverage.** Many behaviors are fully covered by the automated test suite in `src/test/`. Checklists should only contain items that require manual EDH verification — VS Code UI interactions, setting-change wiring, toast content, QuickPick rendering, drag-and-drop integration.
