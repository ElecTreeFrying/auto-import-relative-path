/**
 * Enum representing notification types for error and warning scenarios.
 */
export enum NotifyType {
  /** Indicates that the file path provided is identical to an existing one. */
  SameFilePath,
  /** Indicates that the provided file or operation is not supported. */
  NotSupported,
}
