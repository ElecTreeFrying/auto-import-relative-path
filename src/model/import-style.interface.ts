/**
 * Defines the structure for an import style option.
 */
export interface ImportStyle {
  /** Numeric identifier for the import style. */
  value: number;
  /** The code snippet that describes this import style. */
  description: string;
}
